services:
  jarnkoll:
    build: .
    container_name: jarnkoll
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      NODE_ENV: production
      PORT: 3000
      DATA_DIR: /app/data
      DB_PATH: /app/data/jarnkoll.db
